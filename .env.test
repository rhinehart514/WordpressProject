# Test Environment Configuration
NODE_ENV=test

# Test Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/wordpress_rebuilder_test?schema=public"

# Redis (for testing job queues)
REDIS_URL=redis://localhost:6379/1

# API Configuration
API_PORT=3002
LOG_LEVEL=error

# JWT Configuration (use separate secret for tests)
JWT_SECRET=test_jwt_secret_at_least_32_characters_long_for_testing_purposes
JWT_EXPIRATION=1h

# OpenAI (use test key or mock)
OPENAI_API_KEY=sk-test-key-for-testing-purposes-only
AI_BASE_URL=http://localhost:8080
OPENAI_MODEL=gpt-3.5-turbo

# WordPress (test values)
WORDPRESS_DEFAULT_URL=http://localhost:8080/wp
WORDPRESS_API_KEY=test_api_key

# CORS
ALLOWED_ORIGINS=http://localhost:3000

# Next.js (for reference)
NEXT_PUBLIC_API_URL=http://localhost:3002
NEXTAUTH_SECRET=test_nextauth_secret_at_least_32_characters_long
NEXTAUTH_URL=http://localhost:3000
